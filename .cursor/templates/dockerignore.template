# =============================================================================
# .dockerignore template (from rg-common)
# Copy to your project root as .dockerignore and customize as needed.
# =============================================================================

# Git
.git
.gitignore
.gitmodules

# Python
**/__pycache__
**/*.pyc
**/*.pyo
**/*.pyd
.Python
*.so
*.egg
*.egg-info
dist
build
.venv
venv
env

# Node
node_modules
.pnpm-store

# IDE
.vscode
.idea
*.swp
*.swo
.cursor

# Environment / Secrets
.env
.env.*
!.env.example

# Documentation (not needed in image)
docs
*.md
!README.md

# Tests (not needed in production image)
tests
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov

# OS
.DS_Store
*.log

# rg-common submodule (not needed in image)
rg-common

# Scripts (not needed in production image)
scripts

# Docker compose files (not needed in image)
docker-compose*.yml
docker-compose*.yaml

# Build/setup markers
.setup-complete
